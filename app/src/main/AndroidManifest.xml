<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tpfmassonnicolas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TPFMassonNicolas">

        <activity android:name=".ListMovie"
        android:label="Movie" />
        <activity
            android:name=".AddMovie"
            android:label="@string/Add_Movie" />
        <activity
            android:name=".PageCreateAccount"
            android:label="@string/Sign_Up" />
        <activity
            android:name=".DetailMovie"
            android:label="Detail Movie" />
        <activity
            android:name=".MainActivity"
            android:label="Page Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>